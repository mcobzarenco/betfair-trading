"""

This file deliberately has no Python code.

It allows the Python interpreter use the betfair folder as a module, so we can import directly from it.

"""

from api import API
from api_throttled import API_T
